public class PriceCalculator {
    Client client = new Client();
    // añadir peso y precio por quilo
    public double finalPrice() {
        //return getWeight * getKgPrice * client.getAmount();
        return 0;
    }
}
